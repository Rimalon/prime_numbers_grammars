arch: amd64
os: linux
dist: focal

language: python
python:
  - "3.8"
jobs:
  include:
    - stage: free grammar prime generator
      script:
        - cd turing_machine_generator/
        - python prime_generator.py -n 25
        - cd ..